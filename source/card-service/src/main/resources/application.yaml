server:
  port: 8085

spring:
  application:
    name: card-service

  datasource:
    url: jdbc:postgresql://localhost:5432/offerwall_card
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

offerwall:
  seed:
    enabled: false
  security:
    internal:
      api-key: ${OFFERWALL_INTERNAL_API_KEY:local-dev-internal-key}
    jwt:
      issuer: offerwall-user-service
      audience: offerwall-api
      require-signature: false
